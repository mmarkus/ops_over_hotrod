This project provides support for running various operations over hotrod: e.g. transactions, distributed-executors and map/reduce, queries etc.
These operations are not yet supported by hotrod, but they will be in future releases.
As there is a significant demand from Infinispan's community for running these operations, this project provides a way to run these operations.
Once these operations will be supported natively by hotrod, it
